[Unit]
Description=proxyctl Caddy HTTP router

[Service]
Environment=HOME=/config
ExecStart=/usr/bin/setsid /config/proxyctl/bin/run-caddy
Restart=always
RestartSec=2

[Install]
WantedBy=default.target
